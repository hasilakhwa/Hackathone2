# Frontend Environment Variables for Kubernetes
# Copy to frontend.env and replace placeholders with actual values

# API URLs (use service DNS names in Kubernetes)
NEXT_PUBLIC_API_URL=http://backend-service:8000
NEXT_PUBLIC_MCP_SERVER_URL=http://mcp-service:5000
